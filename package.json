{
  "name": "eve-starmap-monorepo",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alocay/eve-starmap.git"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.1.0",
    "jsdom": "^26.1.0"
  }
}
