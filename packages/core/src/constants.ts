// Radius (px) of the base system dot drawn by StarmapRenderer. Shared with
// layers (e.g. heatmapLayer) so they can size themselves relative to it --
// e.g. a heatmap circle needs to extend past this to stay visible once the
// dot is drawn on top of it.
export const SYSTEM_DOT_RADIUS = 2
